# WaLRuS-DATA — 2026-05-24

Session scope: Bootstrap of `ichnos` — installed the epistegrity scaffold, instantiated the project-specific constitution files, and opened the Phase 0 issue. No Phase 0 work performed.

---

## Wins

- Pulled `METHODOLOGY.md` (133 lines), `ARCHITECTURE.md` (65 lines), `docs/walrus-TEMPLATE.md` (53 lines) verbatim from epistegrity `main`; each verified non-empty and beginning with a `#` heading (not error HTML).
- Pinned upstream sha `8e89b0763c7d0f63e36a7bde73939f34acfbd836` in `.epistegrity-version` for future drift detection.
- Wrote `CLAUDE.md`, `IMPLEMENTATION.md` (Phase 0 → Phase 6), `README.md`, `.gitignore`.
- Two scope-tagged commits: `69671e9` (scaffold + version pin), `c028c94` (project files).
- Opened Phase 0 issue: https://github.com/seva/Ichnos/issues/1
- This WaLRuS committed (`Refs #1`) and branch pushed.

## Learnings

- This is the Claude Code web/cloud sandbox, not the local shell the seeding prompt assumed. Repo was pre-created and cloned to `/home/user/Ichnos` on branch `claude/ichnos-bootstrap-ZPfOA`; the prompt's Step 1 (`gh repo create`) was unnecessary.
- `gh` CLI is NOT available here. All GitHub operations go through MCP tools (`mcp__github__*`), scoped to `seva/ichnos` only — epistegrity cannot be queried via MCP.
- `api.github.com` unauthenticated rate limit was already exhausted (HTTP 403) for the sandbox egress IP, so the prompt's REST call for the sha failed. Resolved with `git ls-remote https://github.com/seva/epistegrity main`, which uses the git protocol and is not subject to the REST limit.
- `raw.githubusercontent.com` fetches work fine from the sandbox; the three scaffold files came down clean.
- Remote canonical name is `seva/Ichnos` (capital I); MCP scope string is `seva/ichnos`. GitHub routes case-insensitively — the issue landed at `seva/Ichnos/issues/1`.

## Risks

- The entire premise rests on Phase 0 Open Question #2: that ≥ minimum-corpus Mythos-credited advisories with publicly retrievable full text actually exist. Unverified. If false, the project cannot proceed past ingestion.
- The external interfaces named in `IMPLEMENTATION.md` (Anthropic Glasswing CVD dashboard, oss-sec Coopersmith threads from May 2026, NVD, GHSA) have not been confirmed to exist or be reachable under this sandbox's network policy. Phase 0 must probe reachability first.
- Sandbox egress is rate-limited and possibly filtered (see the `api.github.com` 403). Phase 0 discovery and Phase 1 ingestion may hit limits.
- `.epistegrity-version` pins a sha but there is no automated drift check yet — drift is only detectable by manual comparison.
- Bootstrap pushed to `claude/ichnos-bootstrap-ZPfOA`, not `main`. The remote default branch may differ; a future session or PR merge must reconcile.

## Strategy

- Next session runs the `CLAUDE.md` session-start protocol and begins Phase 0 against issue #1. Bootstrap is setup only; no discovery was done here.
- Sequence Phase 0 to resolve the existential questions first: confirm the Glasswing CVD dashboard URL and whether it returns advisory text or only IDs (Open Question #1), then corpus availability (#2). These gate everything downstream.
- Cheapest-probe-first: verify each external interface is reachable under the sandbox network policy before designing scrapers.

## Decisions

- Pushed to `claude/ichnos-bootstrap-ZPfOA` instead of `main` (which the seeding prompt's Steps 7/8/10 specified). The session's branch requirement is explicit and the no-unauthorized-push rule prohibits pushing to main without permission; the branch requirement wins. Flagged for the operator.
- Obtained the epistegrity sha via `git ls-remote` after the REST 403 — same value, no auth needed.
- Created the Phase 0 issue with clean markdown backticks rather than the backslash-escaped backticks in the prompt's quoted heredoc; the escapes were shell artifacts that would have rendered literally.
- Did NOT create the seven Phase 0 `docs/` outputs — they are Phase 0 products, not bootstrap artifacts.

## Alignment

- `METHODOLOGY.md` now governs: TDD (tests before code; Phase 0 exempt), commit discipline (one logical unit, scope-tagged, `Closes`/`Refs #N`), failure handling (Hypothesis → online → source code, no blind retry), phase gate (no implementation code against undiscovered interfaces), post-phase audit before each WaLRuS.
- `CLAUDE.md` conventions in force: corpus integrity (logged URL + sha256 per entry, never invent advisory text), frozen held-out splits committed before fitting, two AUCs always reported as a pair with 95% CIs, no mediated stylometry, black-box discipline toward Mythos, immutable append-only `reports/`.
- GitHub work goes through MCP tools only, scoped to `seva/ichnos`.

## Tradeoffs

- Accepted pinning epistegrity by sha with no automated drift check — cheap now, manual to verify later. Acceptable for bootstrap; revisit if the scaffold is re-synced.
- Accepted `git ls-remote` HEAD-of-main as the provenance sha. It matches the fetched files only if `main` did not move between the ls-remote and the curl pulls; both ran seconds apart in this session, so the window is negligible.

## Alternatives

- Authenticating or waiting out the `api.github.com` rate limit to get the sha — rejected; `git ls-remote` is immediate and auth-free.
- Following the prompt literally and pushing to `main` — rejected; violates the session branch requirement and the no-unauthorized-push rule.
- Creating placeholder Phase 0 docs to "fill out" the tree — rejected; the prompt forbids it and placeholders masquerade as discovery.
